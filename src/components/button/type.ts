import { ReactElement } from "react";

type IProps = {
  children?: any;
  onClick?: Function;
  classes?: string;
  disabled?: boolean;
  loading?: boolean;
  id?: string;
  type?: string;
};
export type { IProps };
